# Copy to .env (which is gitignored), then: chmod 600 .env && source .env
# Paper keys only. Paper key IDs begin with PK; live keys begin with AK.
export APCA_API_KEY_ID='PK...'
export APCA_API_SECRET_KEY='...'
